#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <string>
#include <algorithm>
#include <map>
#include <stack>
#include <queue>
#include <vector>
#include <set>
#include <windows.h>

using namespace std;

class wtl {
	public:
		
};

int main() {
	
	string fileName, inName, outName;
	
	cin >> fileName;
	cout << "\nCompiling...\n";
	
	inName = fileName.find(".wtl") ? fileName : fileName + ".wtl";
	outName =
	freopen(fileName + ".wtl", "r", stdin);
	freopen("." + fileName + ".cpp", "w", stdin);
	
	sort(a, a + n, [](int a, int b){
		cmp;
	});
	
	return 0;
	
}
